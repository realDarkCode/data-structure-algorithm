{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Compile and run C",
      "command": "gcc",
      "args": [
        "-g",
        "\"${file}\"",
        "-o",
        "\"${fileDirname}\\${fileBasenameNoExtension}.exe\"",
        "&&",
        "\"${fileDirname}\\${fileBasenameNoExtension}.exe\""
      ]
    }
  ]
}
